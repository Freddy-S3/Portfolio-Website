# PHPStorm
.idea

# LaTeX build artifacts
resume/build/
# Page previews rendered for review. Already covered by resume/build/ above; listed
# explicitly so the intent survives if that line is ever narrowed.
resume/build/renditions/*.png
*.aux
*.log
*.out
*.fdb_latexmk
*.fls
*.synctex.gz

# Playwright screenshots
.screenshots/

# Python bytecode from the tools/ scripts
__pycache__/
*.pyc

# Private working notes - never published
*.local.md

# Cloned mirror used by tools/skills_to_site.py - regenerated, not committed
.harness-cache/
